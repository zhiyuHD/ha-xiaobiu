# Best Practices

- 错误分类优先基于稳定的业务码或可识别消息模式，不要只在 CLI 层用字符串 contains 做临时提示。
- 用户可恢复的错误要给出下一步动作，例如“稍后再试”，而不是只回显原始服务端文案。
- 第三方验证码组件初始化要受用户显式操作控制，避免页面一加载就打断用户。
- 共用 runtime 的 UI/错误处理修复必须同步到 vendored 副本，并由测试锁定行为。
